## What changed

<!-- What this does and why, in a couple of sentences. -->

Fixes #

## How it was tested

<!-- What you clicked through, and anything you could not test. -->

## Screenshots

<!-- Required for panel and embed changes. A short clip works too. Delete this section otherwise. -->

## Needs

- [ ] A migration under `src/lib/migrations`
- [ ] A new `.env` key
- [ ] A bot restart to take effect

## Checklist

- [ ] One concern, branched off `master`.
- [ ] Matches the surrounding code — same naming, same idiom, no inline comments.
- [ ] Bot-facing strings updated in **both** `en.json` and `id.json`, with orphaned keys deleted.
- [ ] Schema changes add a new migration instead of editing an existing one.
- [ ] `npm run build:format`
- [ ] `npm run build:sync`
- [ ] `npm run build`
- [ ] `npx svelte-check`
- [ ] Ran the panel and clicked through the parts I changed.
