services:
  database:
    image: mariadb:10.11
    container_name: dansday-database
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MARIADB_DATABASE: ${DB_NAME}
      MARIADB_USER: ${DB_USER}
      MARIADB_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    ports:
      - "${DB_PORT:-3306}:3306"
    networks:
      - dansday-network
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  app:
    build: .
    container_name: dansday-app
    restart: unless-stopped
    depends_on:
      database:
        condition: service_healthy
    environment:
      DATABASE_URL: mysql://${DB_USER}:${DB_PASSWORD}@database:3306/${DB_NAME}
      CONTROL_PANEL_PORT: ${CONTROL_PANEL_PORT:-80}
      SESSION_SECRET: ${SESSION_SECRET}
      TIMEZONE: ${TIMEZONE}
      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
    ports:
      - "${CONTROL_PANEL_PORT:-80}:${CONTROL_PANEL_PORT:-80}"
    volumes:
      - app_data:/app/data
      - .:/app
      - /app/node_modules
    networks:
      - dansday-network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: dansday-phpmyadmin
    restart: unless-stopped
    depends_on:
      database:
        condition: service_healthy
    environment:
      PMA_HOST: database
      PMA_PORT: 3306
      PMA_USER: ${DB_USER}
      PMA_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
    ports:
      - "8081:80"
    networks:
      - dansday-network

volumes:
  mysql_data:
    driver: local
  app_data:
    driver: local

networks:
  dansday-network:
    driver: bridge

