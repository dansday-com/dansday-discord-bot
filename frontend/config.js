// Frontend Configuration

// Control Panel Configuration
export const CONTROL_PANEL = {
    // Port for control panel web interface
    PORT: 8888,
    // Password for accessing control panel (change this to a secure password)
    PASSWORD: "goblox2025",
    // Enable control panel (set to false to disable)
    ENABLED: true
};

